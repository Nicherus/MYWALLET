import React, { useState } from 'react';
import { MainContainer } from './styles';


export default function AddFlowValue() {
//   const { user, setUser } = useUserContext();

//   }

  return (
    <MainContainer>
    </MainContainer>
  );
}